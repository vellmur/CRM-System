{% extends 'emails/base.html.twig' %}

{% block body %}
    <h4>New client</h4>
    <p>Client name: {{ user.client.name }}</p>
    <p>Username: {{ user.username }}</p>
    <p>Email: {{ user.email }}</p>
{% endblock %}