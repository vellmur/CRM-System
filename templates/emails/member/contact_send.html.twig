{% extends 'emails/base.html.twig' %}

{% block body %}
    <h4>{{ subject }}</h4>

    {% if owner.id %}
        <p>Name: {{ owner.fullname }}</p>
    {% endif %}
    <p>Email: {{ owner.email }}</p>

    {% if message %}
        <p>Message: {{ message }}</p>
    {% endif %}

    {% if owner.id %}
        <p>This is a link to membership panel: <a href="{{ owner|profile_link }}">Profile</a></p>
    {% endif %}
{% endblock %}