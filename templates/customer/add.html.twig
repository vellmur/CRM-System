{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-default">
        <div class="panel-head">
            <h6><i class="icon-user-plus"></i> {{ 'customer.add.lead'|trans({}, 'labels') }}</h6>
        </div>
        <div class="panel-body">
            {{ include ("customer/forms/member.html.twig") }}
        </div>
    </div>
{% endblock body %}